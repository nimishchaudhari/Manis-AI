{
  "name": "@acme/agent-template",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@acme/shared-mcp": "workspace:*",
    "@acme/shared-utils": "workspace:*",
    "amqplib": "^0.10.3"
  },
  "devDependencies": {
    "@types/amqplib": "^0.10.3",
    "vitest": "^1.6.1"
  }
}
